# mkosi / build artifacts
*.raw
*.qcow2
*.img
*.vmdk
*.vdi
*.initrd
*.vmlinuz
output/
mkosi.output/
mkosi.cache/
mkosi.builddir/
.mkosi-*
*.efi

# mkosi "latest" output symlink (-> thriveos.raw)
/thriveos
/sprout

# sprout-pi: mkosi Format=directory rootfs tree (root-owned, ~2GB build output,
# NOT source). The pi/ build definition (mkosi.conf, build.sh, mkosi.extra, …) is
# tracked; only the assembled rootfs is ignored.
pi/sprout-pi/
