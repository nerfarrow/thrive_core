# Same appliance auto-login as tty1, for the serial console (virsh console /
# headless host). See getty@tty1.service.d/autologin.conf.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin root --keep-baud 115200,57600,38400,9600 %I $TERM
