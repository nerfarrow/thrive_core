[Daemon]
# the Thrive splash (usr/share/plymouth/themes/thrive). Only actually shown in the
# `user` image, whose kernel cmdline carries `quiet splash`; the `dev` image leaves
# the verbose console on, so plymouth never activates even though it's installed.
Theme=thrive
