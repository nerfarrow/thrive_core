data/
__pycache__/
*.pyc
node_modules/
dist/
.env
*.db

# modules: only the bundled `users` module ships with thrive_core.
# Everything else (vehicles, budget, vault, ...) is installed separately.
modules/*
!modules/users/